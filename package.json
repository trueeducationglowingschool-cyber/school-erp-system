{
  "name": "school-erp-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create school-erp-db",
    "db:migrate": "wrangler d1 migrations apply school-erp-db",
    "db:seed": "wrangler d1 execute school-erp-db --file=migrations/0001_init.sql"
  },
  "devDependencies": {
    "wrangler": "^3.90.0",
    "@cloudflare/workers-types": "^4.20250101.0"
  }
}
